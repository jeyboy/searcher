-#:javascript
-#  $(document).keydown(function(event) {
-#    if (event.which == 17) {
-#
-#    }
-#  });
-#
-#  $(document).keyup(function(event) {
-#    if (event.which == 17) {
-#
-#    }
-#  });

.db.oh.left
  = render :partial => "tags/cloud", :object => @cloud

.db.w70{:style => "padding-left: 30%"}
  #page_content.db.w100
.cb