doctype html
html lang="en"
  head
    = render 'layouts/head'
  body
    .content
      = render 'layouts/navbar'

      .container-fluid
        .row-fluid
          .span3.sidebar
            .vblock
              .well.sidebar-nav
                = render 'layouts/categories'
            .vblock
              .well.sidebar-nav
                = render 'layouts/tags'

          .span9.posts_body
            = bootstrap_flash
            = yield

    = render 'layouts/footer'
