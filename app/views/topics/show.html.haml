= render :partial => "shared/highlight"

%h2.bg.content_block.cw.tac.topic_title= @topic.name

#posts
  = render :partial => "shared/paginate", :locals => {:objects => @posts}
  = render @posts
  = render :partial => "shared/paginate", :locals => {:objects => @posts}